apiVersion: apps/v1
kind: Deployment
metadata:
  name: pma-worker
spec:
  template:
    spec:
      containers:
        - name: worker
          image: ghcr.io/personal-pm/worker:latest
          env:
            - name: APP_ENVIRONMENT
              value: production
